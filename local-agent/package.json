{
  "name": "printly-local-agent",
  "version": "1.0.0",
  "description": "Standalone inventory agent for Printly — polls orders, deducts stock, logs changes",
  "private": true,
  "scripts": {
    "start": "npx tsx index.ts",
    "dev": "npx tsx watch index.ts",
    "test": "npx tsx test/consumption.test.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
